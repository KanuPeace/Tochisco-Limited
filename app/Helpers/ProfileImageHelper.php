<?php

namespace App\Helper;

use Illuminate\Http\Request;

class ProfileImageHelper
{
  public static function saveProfileImage($request)

  {

    // $profile_path  = uniqid() . '_' . $request->title . '.' .
    //   $request->profile_image->extension();
    // $request->image->move(public_path($request),  $profile_path);
  }
}
